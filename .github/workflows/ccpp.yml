name: C/C++ CI

on:
  push:
    branches: 
      - master

jobs:
  build:
    name: test build
    runs-on:  [ubuntu-latest]
    
    steps:
    - name: configure
      run: cmake .
    - name: make
      run: make

